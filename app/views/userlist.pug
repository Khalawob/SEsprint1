html 
    head    
        title Userlist
    body
       h1 Userlist 
       p This is our user list page it show you the list of users from the database and there infomation 
       h2 Names
       - console.log(data)
       .main-content 
       table(border=1)
            for item in data 
                tr  
                    td #{item.User_ID}
                    td 
                        a(href='/student-single/' + item.id) #{item.User_Name}